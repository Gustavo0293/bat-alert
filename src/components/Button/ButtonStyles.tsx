import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  button: {
    padding: 10,
    fontSize: 20,
    fontWeight: 'bold',
    color: 'white',
    borderRadius: 12,
    width: 220,
    textAlign: 'center',
    marginTop: 20
  }
});