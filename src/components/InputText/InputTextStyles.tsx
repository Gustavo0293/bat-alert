import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  input: {
    backgroundColor: 'gray',
    color: 'black',
    padding: 10,
    paddingLeft: 30,
    borderRadius: 15,
    borderWidth: 2,
    width: '100%',
    marginTop: -30
  }
});